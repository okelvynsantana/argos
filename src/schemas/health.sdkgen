@description Resposta padrão da api
type ApiResponse {
  
  status: int
  message: string
  error: bool
}

@description Return a message when api is running
fn checkApiStatus(): ApiResponse

error InternalServerError {
  message: string
}